- if resource.present? && resource.errors.any?
  = content_tag :div, class: 'resource-errors' do
    %h4 #{pluralize(resource.errors.count, 'error')} detected
    %ul
      - resource.errors.full_messages.each do |msg|
        %li.text-danger= msg